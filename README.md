# Build Local

1. voltaのダウンロード  
```bash
curl https://get.volta.sh | bash
```
```bash
source ~/.bashrc # または ~/.zshrc
```

2. nodejs、yarnとパッケージをインストール  
```bash
yarn install
```

3. ローカルサーバーを立ち上げる
```bash
yarn dev
```
